DEEPGRAM_API_KEY=
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Required for the Recall.ai Meet-bot join path (see SETUP.md step 2).
# Not needed to run demo_standalone.py.
RECALL_API_KEY=
# Your ngrok reserved domain -- hostname only, no "https://" prefix.
RECALL_WEBSOCKET_HOSTNAME=
# Optional. Without it mia runs normally with Logfire reporting disabled.
# LOGFIRE_TOKEN=
# Optional overrides:
# WAKE_WORD="hey mia"
# FUZZY_THRESHOLD=0.75
# RECALL_BASE_URL=https://us-west-2.recall.ai
# RECALL_WEBSOCKET_PORT=8765
# RECALL_BOT_NAME=Mia
# Extra spellings that count as the wake word, matched exactly. Speech-to-text
# returns these for "hey mia" often enough to cost a repeat otherwise. "amy" is
# deliberately absent: it is also a person's name.
# WAKE_ALIASES=mia,hemia,hamia,miya,maya
